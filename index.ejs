<script src="https://code.jquery.com/jquery-1.10.2.js"></script>

<script>
    var items = [];
$.ajax({ 
    type: 'GET', 
    url: 'js/data.json', 
    data: { get_param: 'value' }, 
    dataType: 'json',
    success: function (data) { 
        $.each(data, function(index, element) {
            items.push( "<div class='video'><figure><img class='thumbnail' src='" + element.thumbnail + "'><figcaption>"+element.label+"</figcaption></figure></div>" );
        });
        
        $( "<div/>", {
            "class": "videos-container",
            html: items.join( "" )
          }).appendTo( "#container" );
            }
});
</script>


<section class="video-library">
  <div class="container" id="container">
  </div>
</section>